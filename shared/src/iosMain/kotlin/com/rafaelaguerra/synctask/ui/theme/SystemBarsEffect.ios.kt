package com.rafaelaguerra.synctask.ui.theme

import androidx.compose.runtime.Composable

@Composable
actual fun SystemBarsEffect(useDarkIcons: Boolean) {
    // Status bar appearance on iOS is driven by the hosting UIViewController
    // (see ComposeUIViewController configuration in iosMain).
}
